package com.huayu.irla.mobile.latestCourse;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;

/**
 * @Description 小程序最新课程
 * @author 顾广婷
 * @data 2018-08-30
 */
@Controller
@RequestMapping("/latestCourseAction")
public class LatestCourseAction {
	
/*	private Logger logger = Logger.getLogger(LatestCourseAction.class);
	
	@Autowired
	private ICourseService courseService;*/
	
/*	@ResponseBody
	@RequestMapping(value = "/latestCourseShow", method = RequestMethod.GET)
	public ResultVO activeShow(HttpServletRequest request, HttpServletResponse response) {
		ResultVO result = new ResultVO();
		try {
			request.setCharacterEncoding("utf-8");
			response.setCharacterEncoding("UTF-8");
			response.setContentType("text/html;charset=utf-8");
			CourseVO course = courseService.getNewCourse();
			result.setNetCode(200);
			result.setData(course);
		} catch (UnsupportedEncodingException e) {
			result.setNetCode(500);
			logger.error(e);
		}
		return result;
	}*/
}
